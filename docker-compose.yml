services:
  mongodb:
    image: mongo:8
    container_name: place-scout-db
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: place-scout
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: